generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

// TODO: Delete
model User {
  id    String     @id @unique
  gender String?
  sport String?
}

